#include <gtest/gtest.h>

#include "stringtree.h"

// Instantiate, check 
TEST(StringTreeTest, NewDestroy) {
  // freemap_t *map = freemap_new(127);

  // EXPECT_NE(map->bitmap, nullptr);
  // EXPECT_EQ(map->total, 127);
  // EXPECT_EQ(map->free, 127);

  // freemap_destroy(map);
}
